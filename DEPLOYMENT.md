# Deployment

Repository: `gkhanna/gravity`
GitHub Pages source: `master` branch, repository root.

Large media note: videos are included because each file is below GitHub's hard 100 MB object limit.

Excluded from public deployment:
- `dokuwiki/` runtime tree
- `wfserver/` PHP/runtime/output tree
- generated output directories and binaries
